-- script that lists all rows of the table 'first_table'
-- the table is from db 'hbtn_0c_0'

-- all fields should be printed
-- the db name will be passed as an argument to the mysql command
SELECT *
	FROM first_table;
