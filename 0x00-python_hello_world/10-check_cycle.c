#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
 * check_cycle - checks if a singly linked list has a cycle in it
 * @list: points to the first node of the list
 *
 * Return: O if there is no cycle, else 1
 */
int check_cycle(listint_t *list)
{
	return (1);
}
